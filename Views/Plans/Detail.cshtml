
<section class="plan-info">
    <div class="row">
    <div class="col-md-9">
        <h1>Title</h1>
    </div>
    <div class="col-md-3 d-flex align-items-end">
        <a href="#" class="btn btn-warning">Start Plan</a>
        </div>
    </div>
    <div class="row">
        <div class="col-md-9">
            <p>Author</p>
        </div>
        <div class="col-md-3">
            <aside>
                <author>Author</author>
                    <div class="rating" title="Rating">
                       @* @for(int i = 1; i<= 5; i++)
            {
                if(Model.Rating >= i)
                {
                    
                    <i class="fas fa-star"></i>
                }
                else if(Model.Rating > i -1)
                {
                    <i class="fas fa-star-half-alt"></i>
                }
                else 
                {
                    <i class="far fa-star"></i>
                }
            } *@
                    </div>
                    <div class="duration">
                       <i class="far fa-clock"></i><time>1m 30s</time>
                        <br></div>
                    <div class="price">
                        @* @Model.CurrentPrice
                        @if(!Model.CurrentPrice.Equals(Model.FullPrice))
                            {
                                <br><s>@Model.FullPrice</s>
                            } *@
                        <br></div>
                    <img src="/logo.png" width="30"/>
            </aside>
        </div>
    </div>
</section>

<section class="exercises">
    <h2>
        Exercises@* Lezioni (@Model.Lessons.Count) *@
    </h2>
    @* @foreach (var lesson in Model.Lessons)
    { *@
    @for(int i = 0; i< 8; i++)
    {
        <hr>
        
        <div class="row">
            <div class="col-md-9 d-flex align-items-center ">
                <a href="#">Exercise @i</a>
            </div>
            <div class="col-md-3 d-flex align-items-center exercise-duration">
                <i class="far fa-clock"></i><time>1m 30s</time>
            </div>
        </div>
    @* } *@
    }
        </hr>

        <div class="row plan-duration">
            <div class="col-md-9 d-flex align-items-center">
                Totale
            </div>
            <div class="col-md-3 d-flex align-items-center plan-duration">
                <i class="far fa-clock"></i><time>1h 1m 30s</time>
            </div>
        </div>
    
</section>
    