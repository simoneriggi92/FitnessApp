
<section class="course-info">
    <div class="row">
    <div class="col-md-9">
        <h1>Title</h1>
    </div>
    <div class="col-md-3 d-flex align-items-end">
        <a href="#" class="btn btn-warning">Iscriviti</a>
        </div>
    </div>
    <div class="row">
        <div class="col-md-9">
            <p>Author</p>
        </div>
        <div class="col-md-3">
            <aside>
                <author>Author</author>
                    <div class="rating" title="Rating">
                       @* @for(int i = 1; i<= 5; i++)
            {
                if(Model.Rating >= i)
                {
                    <i class="fas fa-star"></i>
                }
                else if(Model.Rating > i -1)
                {
                    <i class="fas fa-star-half-alt"></i>
                }
                else 
                {
                    <i class="far fa-star"></i>
                }
            } *@
                    </div>
                    <div class="price">
                        @* @Model.CurrentPrice
                        @if(!Model.CurrentPrice.Equals(Model.FullPrice))
                            {
                                <br><s>@Model.FullPrice</s>
                            } *@
                        <br></div>
                    <img src="/logo.png" width="30"/>
            </aside>
        </div>
    </div>
</section>

<section class="course-lessons">
    <h2>
        Lezioni@* Lezioni (@Model.Lessons.Count) *@
    </h2>
    @* @foreach (var lesson in Model.Lessons)
    { *@
        <hr>
        
        <div class="row">
            <div class="col-md-9 d-flex align-items-center ">
                <a href="#">Title</a>
            </div>
            <div>
                <i class="far fa-clock"><time>Duration</time></i>
            </div>
            </div>
    @* } *@
        </hr>

        <div class="row course-duration">
            <div class="col-md-9 d-flex align-items-center">
                Totale
            </div>
            <div class="col-md-3 d-flex align-items-center">
                <i class="far fa-clock"></i>
            </div>
        </div>
    
</section>
    